#include <unistd.h>

int ft_iterative_factorial(int nb)
{
    while(x != 0)
    {
        if(nb < -)
        nb = (nb * nb -1)
    }

    return (0);

    
}

int main()
{
    ft_iterative_factorial(4);
    return (0);
}